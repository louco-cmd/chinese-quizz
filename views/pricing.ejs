<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upgrade to Premium - Chinese Quiz</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <style>
    .pricing-container {
      min-height: 80vh;
      display: flex;
      flex-direction: column;
    }
    
    .card {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      border-radius: 12px;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      height: 100%;
      border: 1px solid #e0e0e0;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1) !important;
    }

    .card-title {
      font-weight: 700;
      color: #333;
    }

    .price-display {
      color: #0d6efd;
      margin: 1.5rem 0;
    }

    .feature-list li {
      padding: 0.5rem 0;
      border-bottom: 1px solid #f0f0f0;
    }

    .feature-list li:last-child {
      border-bottom: none;
    }

    .badge-default {
      background-color: #6c757d;
      color: white;
      font-size: 0.7rem;
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
    }

    .cancel-notice {
      font-size: 0.85rem;
      color: #6c757d;
      margin-top: 0.5rem;
    }

    .btn-action {
      padding: 0.75rem 1.5rem;
      font-weight: 600;
      border-radius: 8px;
      transition: all 0.3s ease;
      width: 100%;
      margin-top: auto;
    }

    .btn-action:hover {
      transform: scale(1.05);
    }

    .plan-card-body {
      display: flex;
      flex-direction: column;
      flex: 1;
    }

    .plan-features {
      flex: 1;
    }

    .badge-previous {
      background: linear-gradient(45deg, #6c757d, #495057);
      color: white;
      font-size: 0.7rem;
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      border: 1px solid #495057;
    }

    .btn-reactivate {
      background: linear-gradient(45deg, #ffc107, #ff9800);
      color: white;
      border: none;
      font-weight: 600;
    }

    .btn-reactivate:hover {
      background: linear-gradient(45deg, #ff9800, #ff5722);
      color: white;
      transform: scale(1.05);
    }

    .subscription-status-badge {
      font-size: 0.75rem;
      padding: 0.2rem 0.6rem;
      border-radius: 10px;
      margin-left: 0.5rem;
    }
    
    .premium-card {
      border: 2px solid #0d6efd !important;
    }
    
    .card-header-premium {
      background: linear-gradient(45deg, #0d6efd, #0a58ca);
      color: white;
    }
    
    /* Mobile specific styles */
    @media (max-width: 767.98px) {
      .price-display {
        margin: 1rem 0;
      }
      
      .card {
        margin-bottom: 1.5rem;
      }
      
      .btn-action {
        padding: 0.75rem 1rem;
      }
    }
  </style>
</head>

<body>
  <% // Variables locales 
    const currentUser = locals.user || null; 
    const currentBalance = locals.balance || 0; 
    const isPremium = locals.isPremium || false;
    const hasInactiveSubscription = locals.hasInactiveSubscription || false;
    const subscriptionStatus = locals.subscriptionStatus || 'none';
    const userSubscription = locals.userSubscription || null;
  %>

  <%- include('partials/header', {currentPage: 'pricing', user: currentUser, balance: currentBalance}) %>
  <%- include('partials/navbar', {currentPage: 'pricing', user: currentUser}) %>

  <div class="container py-5 pricing-container">
    <div class="text-center mb-5">
      <h1 class="display-5 fw-bold mb-3">Unlock Your Chinese Potential</h1>
      <p class="lead text-muted">Choose the plan that's right for you</p>

      <% if (hasInactiveSubscription) { %>
      <div class="alert alert-warning alert-dismissible fade show mx-auto" style="max-width: 600px;" role="alert">
        <i class="bi bi-clock-history me-2"></i>
        <strong>You previously had a Premium subscription</strong>
        <% if (subscriptionStatus === 'expired') { %>
          that expired on <%= userSubscription.current_period_end ? new Date(userSubscription.current_period_end).toLocaleDateString() : 'a previous date' %>.
        <% } else if (subscriptionStatus === 'canceled') { %>
          that was canceled. You can reactivate it anytime.
        <% } else if (subscriptionStatus === 'past_due') { %>
          that has a past due payment. Please update your payment method.
        <% } else if (subscriptionStatus === 'unpaid') { %>
          that is unpaid. Please update your payment method.
        <% } else { %>
          that is currently inactive (status: <%= subscriptionStatus %>).
        <% } %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      <% } %>
    </div>

    <div class="row justify-content-center g-4 mb-5">
      <!-- Premium Plan - AFFICHÉ EN PREMIER SUR MOBILE -->
      <div class="col-md-5 col-lg-4 order-1 order-md-2">
        <div class="card premium-card h-100 shadow">
          <!-- Recommended Badge -->
          <div class="card-header card-header-premium text-center py-3">
            <h5 class="mb-0">
              <i class="bi bi-star-fill me-2"></i>
              Recommended
            </h5>
          </div>

          <div class="card-body p-4 d-flex flex-column">
            <!-- Plan Header -->
            <div class="text-center mb-4">
              <h3 class="card-title fw-bold text-primary">Premium</h3>
              <div class="price-display">
                <span class="display-4 fw-bold text-primary">€3</span>
                <span class="text-muted">/month</span>
                <div class="cancel-notice">
                  <i class="bi bi-arrow-clockwise me-1"></i>
                  Cancel anytime
                </div>
              </div>

              <!-- Plan Status Badges -->
              <div class="mb-3">
                <% if (isPremium) { %>
                  <span class="badge bg-success">Active Plan</span>
                  <% if (userSubscription?.cancel_at_period_end) { %>
                  <span class="badge bg-warning text-dark subscription-status-badge">
                    <i class="bi bi-calendar-x me-1"></i>
                    <% if (userSubscription.current_period_end) { %>
                      Cancels on <%= new Date(userSubscription.current_period_end).toLocaleDateString() %>
                    <% } else { %>
                      Will cancel at period end
                    <% } %>
                  </span>
                  <% } %>
                <% } else if (userSubscription) { %>
                  <!-- A un enregistrement d'abonnement mais n'est pas premium -->
                  <% if (subscriptionStatus === 'expired') { %>
                    <span class="badge bg-secondary">Expired</span>
                  <% } else if (subscriptionStatus === 'canceled') { %>
                    <span class="badge bg-secondary">Canceled</span>
                  <% } else if (subscriptionStatus === 'past_due') { %>
                    <span class="badge bg-danger">Past Due</span>
                  <% } else if (subscriptionStatus === 'unpaid') { %>
                    <span class="badge bg-danger">Unpaid</span>
                  <% } else { %>
                    <span class="badge bg-warning">Inactive (<%= subscriptionStatus %>)</span>
                  <% } %>
                <% } %>
              </div>
            </div>

            <!-- Features List -->
            <div class="plan-features">
              <ul class="feature-list list-unstyled mb-4">
                <li class="d-flex align-items-center">
                  <i class="bi bi-infinity text-primary me-3 fs-4"></i>
                  <div>
                    <strong>Unlimited words</strong>
                    <div class="text-muted small">Learn without limits</div>
                  </div>
                </li>
                <li class="d-flex align-items-center">
                  <i class="bi bi-infinity text-primary me-3 fs-4"></i>
                  <div>
                    <strong>Unlimited duels</strong>
                    <div class="text-muted small">Challenge anytime</div>
                  </div>
                </li>
                <li class="d-flex align-items-center">
                  <i class="bi bi-infinity text-primary me-3 fs-4"></i>
                  <div>
                    <strong>Unlimited quizzes</strong>
                    <div class="text-muted small">Practice endlessly</div>
                  </div>
                </li>
              </ul>
            </div>

            <!-- Action Button -->
            <div class="mt-auto pt-3">
              <% if (isPremium) { %>
                <!-- Pour les utilisateurs Premium actifs -->
                <form action="/create-portal-session" method="POST">
                  <button type="submit" class="btn btn-outline-primary btn-action">
                    <i class="bi bi-gear me-2"></i>
                    Manage my subscription
                  </button>
                </form>
              <% } else if (userSubscription && userSubscription.stripe_customer_id) { %>
                <!-- Pour les utilisateurs avec abonnement inactif mais un customer_id Stripe -->
                <% if (['canceled', 'expired', 'past_due', 'unpaid'].includes(subscriptionStatus)) { %>
                  <form action="/create-portal-session" method="POST">
                    <button type="submit" class="btn btn-warning btn-action">
                      <i class="bi bi-arrow-clockwise me-2"></i>
                      Reactivate your plan
                    </button>
                  </form>
                <% } else { %>
                  <!-- Autres statuts inactifs -->
                  <a href="/subscribe?plan=premium" class="btn btn-primary btn-action">
                    <i class="bi bi-rocket-takeoff me-2"></i>
                    Subscribe to Premium
                  </a>
                <% } %>
              <% } else { %>
                <!-- Pour les utilisateurs Free sans historique d'abonnement -->
                <a href="/subscribe?plan=premium" class="btn btn-primary btn-action">
                  <i class="bi bi-rocket-takeoff me-2"></i>
                  Subscribe to Premium
                </a>
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <!-- Free Plan - AFFICHÉ EN DEUXIÈME SUR MOBILE -->
      <div class="col-md-5 col-lg-4 order-2 order-md-1">
        <div class="card h-100">
          <div class="card-body p-4 d-flex flex-column">
            <!-- Plan Header -->
            <div class="text-center mb-4">
              <h3 class="card-title fw-bold">Free</h3>
              <div class="price-display">
                <span class="display-4 fw-bold">€0</span>
                <span class="text-muted">/month</span>
              </div>

              <!-- Current Plan Badge -->
              <% if (!isPremium) { %>
              <div class="mb-3">
                <span class="badge-default">Current Plan</span>
              </div>
              <% } %>
            </div>

            <!-- Features List -->
            <div class="plan-features">
              <ul class="feature-list list-unstyled mb-4">
                <li class="d-flex align-items-center">
                  <i class="bi bi-check-circle-fill text-success me-3 fs-5"></i>
                  <div>
                    <strong>100 words</strong>
                    <div class="text-muted small">Vocabulary limit</div>
                  </div>
                </li>
                <li class="d-flex align-items-center">
                  <i class="bi bi-check-circle-fill text-success me-3 fs-5"></i>
                  <div>
                    <strong>1 duel/day</strong>
                    <div class="text-muted small">Challenge friends</div>
                  </div>
                </li>
                <li class="d-flex align-items-center">
                  <i class="bi bi-check-circle-fill text-success me-3 fs-5"></i>
                  <div>
                    <strong>2 quizzes/day</strong>
                    <div class="text-muted small">Practice daily</div>
                  </div>
                </li>
              </ul>
            </div>

            <!-- Action Button -->
            <div class="mt-auto pt-3">
              <% if (!isPremium) { %>
              <button class="btn btn-outline-secondary btn-action" disabled>
                <i class="bi bi-check-circle me-2"></i>
                Current Plan
              </button>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- FAQ Section -->
    <div class="row justify-content-center mt-5">
      <div class="col-md-8">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title mb-4">
              <i class="bi bi-question-circle text-primary me-2"></i>
              Frequently Asked Questions
            </h5>

            <div class="accordion" id="faqAccordion">
              <div class="accordion-item">
                <h2 class="accordion-header">
                  <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq1">
                    Can I switch between plans?
                  </button>
                </h2>
                <div id="faq1" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                  <div class="accordion-body">
                    Yes! You can upgrade to Premium anytime. If you cancel Premium, you'll revert to the Free plan
                    at the end of your billing period.
                  </div>
                </div>
              </div>

              <div class="accordion-item">
                <h2 class="accordion-header">
                  <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq2">
                    What payment methods do you accept?
                  </button>
                </h2>
                <div id="faq2" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                  <div class="accordion-body">
                    We accept all major credit cards (Visa, MasterCard, American Express) through our secure
                    Stripe payment system.
                  </div>
                </div>
              </div>

              <div class="accordion-item">
                <h2 class="accordion-header">
                  <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq3">
                    Can I cancel my subscription?
                  </button>
                </h2>
                <div id="faq3" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                  <div class="accordion-body">
                    Absolutely. You can cancel your Premium subscription at any time. You'll continue to have
                    access until the end of your billing period, then revert to the Free plan.
                  </div>
                </div>
              </div>

              <!-- Nouvelle FAQ pour la réactivation -->
              <div class="accordion-item">
                <h2 class="accordion-header">
                  <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq4">
                    Can I reactivate a canceled subscription?
                  </button>
                </h2>
                <div id="faq4" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                  <div class="accordion-body">
                    Yes! If you previously had a Premium subscription, you can reactivate it anytime by clicking the
                    "Reactivate your plan" button. This will restore your Premium access immediately and billing will
                    resume from your previous payment method.
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    // Gestion du bouton "Manage my subscription" et "Reactivate your plan"
    document.addEventListener('DOMContentLoaded', function() {
      const manageForms = document.querySelectorAll('form[action="/create-portal-session"]');
      
      manageForms.forEach(form => {
        form.addEventListener('submit', async function(e) {
          e.preventDefault();
          
          const button = this.querySelector('button[type="submit"]');
          const originalText = button.innerHTML;
          button.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Loading...';
          button.disabled = true;
          
          try {
            const response = await fetch('/create-portal-session', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json',
              },
              credentials: 'include'
            });
            
            const data = await response.json();
            
            if (data.url) {
              window.location.href = data.url;
            } else {
              alert('Error: Unable to load subscription management page');
              button.innerHTML = originalText;
              button.disabled = false;
            }
          } catch (err) {
            console.error('Manage subscription error:', err);
            alert('An error occurred. Please try again.');
            button.innerHTML = originalText;
            button.disabled = false;
          }
        });
      });
    });
  </script>
</body>
</html>